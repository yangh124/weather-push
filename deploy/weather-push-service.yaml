apiVersion: v1
kind: Service
metadata:
  name: weather-push-service
  namespace: default
spec:
  type: ClusterIP
  selector:
    app: weather-push
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080